import type { RouteObject } from 'react-router-dom';

// Public routes have been moved to src/routes/default.tsx under DefaultLayout
// Leaving clientRoutes empty for now to avoid route duplication/conflicts
const clientRoutes: RouteObject[] = [];

export default clientRoutes;
